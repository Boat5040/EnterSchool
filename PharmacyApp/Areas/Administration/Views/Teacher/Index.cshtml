@model EnterSchoolApp.Areas.Administration.Models.Teacher.TeacherViewModel
@{
    Layout = "~/Areas/Administration/Views/Shared/__layoutAdmin.cshtml";
     ViewBag.Title = "Teachers";
}

<div class="wrapper wrapper-content">
    <div id="page-wrapper" class="gray-bg">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox ibox-content">
                        <nav class="navbar-default navbar-static-side" role="navigation">
                            <div class="sidebar-collapse">
                                <ul class="nav" id="side-menu">
                                    <li class="nav-header">

                                    </li>
                                    <li class="@Html.IsSelected(ViewContext, "Teacher")">
                                        <a href="@Url.Action("ListTeachers","Teacher",new { area="Administration"})"><i class="fa fa-users"></i> <span class="nav-label">List Teachers</span></a>
                                    </li>
                                    <li class="@Html.IsSelected(ViewContext, "Teacher")">
                                        <a href="@Url.Action("Createteacher","Teacher",new { area="Administration"})"><i class="fa fa-users"></i> <span class="nav-label">Create Teacher</span></a>
                                    </li>

                                </ul>
                            </div>
                        </nav>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
